(function(){'use strict';function show(){const all=NIGHTGLASS_READER.comparisons,id=location.hash.slice(1),c=all.find(x=>x.id===id)||all[0];const nav=document.getElementById('methods');nav.replaceChildren();for(const x of all){const a=document.createElement('a');a.href='#'+x.id;a.textContent=x.id+' · '+x.title;if(x.id===c.id)a.setAttribute('aria-current','page');nav.append(a);}document.getElementById('method-title').textContent=c.title;document.getElementById('status').textContent=c.available===6?'Six illustrated beats available for comparison. Selections and lettering are provisional; read the separate observed findings.':`Comparison in progress — ${c.available} of 6 illustrated beats available. Missing beats remain marked; this is not a complete sequence.`;const stage=document.getElementById('comic');stage.replaceChildren();for(const p of c.panels){if(p.available)stage.append(ComicReading.renderPanel(p));else{const n=document.createElement('p');n.className='missing';n.textContent=p.story_id+' — artwork not yet available.';stage.append(n);}}}addEventListener('hashchange',()=>{show();scrollTo(0,0)});show();})();
