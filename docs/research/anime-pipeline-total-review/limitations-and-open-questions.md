# Limitations and open questions

## Evidence gaps

- This clean worktree has **no** `experiments/` rasters. Phone readers in a checkout of this branch show overlays without art unless sibling worktrees are present. Browser QA of Ember volume on this tree will show broken source images; that is a production fact, not a QA miss of this package’s hub (the hub does not embed those rasters).
- Not every panel of every chapter was opened at full resolution. Contacts plus targeted full plates were used. Visual-a and visual-b sampled independently.
- North Garden ComfyUI output trees were not fully walked (protocol: do not traverse `ComfyUI/`). Kitchen accepted plates and garden-work previz were inspected.
- Visual-b scorecard justifications are empty in some North Garden lettering cells that scored 2–3 despite no balloons. Lead prefers visual-a’s zeros there.
- Comparable-works panel-level craft is `not_assessed`. Official listings, interviews, and publisher pages were used.
- KakaoPage series homes did not return readable episode copy in fetch (JS app).
- Naver 690px / Kakao 720px cut widths are practitioner/Celsys tradition, **not** re-confirmed on 2026 official help pages (industry draft).
- WEBTOON Canvas file-size article URL currently redirects to thumbnail help; 800×1280 auto-slice is from the Zendesk file-size overview snippet and industry draft citations — treat as official-but-verify-at-upload.
- Tapas official pages disagree on per-file cap (2MB vs 10MB). Use the episode-publish article (940px, 10MB) and re-check at upload.
- Built-in ImageGen `model` / `endpoint` / `usage` / `seed` / monetary cost are **null**. Do not claim $0 cost for that route.
- Historical paid spend **$1.057377** is the G07 bakeoff ledger only.
- `human_minutes` is null. All human-time figures in this package are labeled estimates.
- Owner’s remaining gap is still partly inarticulate. This review operationalizes it; it does not replace an owner pass on the 14-panel pilot.
- Prior uncommitted review at `anime-pipeline-total-review-20260905-092558` exists; it was a path index only, not authority. Independent inspection reached a similar strategic shape; that is noted, not copied as evidence.
- No continuous-goal token API exists in this environment (tasks MCP is scheduled automations). Token/elapsed usage from a goal system is **unavailable**.
- Red-team file may land after this draft; `red-team-review.md` records disposition.

## Rights / licensing

- Ember owner-approval: `commercial_clearance: false`, `copyright_ownership_claimed: false`.
- USCO Part 2 (2025-01-29): prompts alone are not authorship.
- NoobAI-named local files remain commercially blocked per GAP_ANALYSIS and model card (economics draft).
- Arial is requested in SVGs and is not a bundled licensed font.
- Tapas guidelines still ban AI (economics draft). WEBTOON Canvas community policy fetch (2025-11-25 update) had **no** AI-disclosure clause; Originals contracts unread. Korean AI law (ANN 2026-01-24) may require platform disclosure for Korean users.
- Adult LoRA training images are local-only; no signed consent packet found.

## Unresolved owner choices

1. Authorize the 14-panel pilot or not.
2. Ember faces vs new originals on that pilot.
3. Whether any owner hours will be spent (kill criterion if not).
4. Paid renderer authorization (default no).
5. Commercial/platform target (Canvas vs Originals vs Tapas vs none).
