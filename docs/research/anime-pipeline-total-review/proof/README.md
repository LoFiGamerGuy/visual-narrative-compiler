# Static 14-panel proof (no generation)

This assignment’s attempt budget for new rasters is $0 and in-product generation was not used. The prompt allows a fully specified experiment plan plus a static storyboard/lettering proof when a fair image bakeoff cannot be run.

Nothing here overwrites prior pipeline outputs. Characters are original adults for the *neutral* payload. Ember faces are optional in the next-pilot spec and are **not** drawn here.

Open [`index.html`](index.html).
