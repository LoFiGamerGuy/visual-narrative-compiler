# Next experiment queue

Priority reflects expected information gain relative to local effort/cost after the `baseline_legacy` failure profile. It does not promote any renderer. No model download, cloud spend, or sensitive external upload is authorized by this document.

| Priority | Candidate | Measured baseline failure it probes | Current evidence / gate | Required approval before execution |
| --- | --- | --- | --- | --- |
| completed | `flux2_klein_tile_marker_control` | Whether non-figurative marker order/count can serve as a renderer-facing spatial diagnostic | Tiles establish a bounded diagnostic; the r2 Blender control also passes a paired proxy smoke, but FLUX fails no-change. More same-method samples have low information gain. | None used. |
| completed | `neutral-anchor-world-to-comic-panel-map` | Whether the shared kitchen stage can map neutral world positions into frozen panel-facing role layouts without hidden left/right inversion | r2 neutral anchors and an explicit diagnostic world-to-panel map passed a paired FLUX proxy control (2/2) without changing gauntlet semantics. FLUX no-change then globally drifted (99.986% changed pixels), so it is not a repair route. | None used. |
| completed | `illustrious_xinsir_target_nochange_matrix` | Whether lower denoise can preserve an unchanged target while still executing a localized change | At strength 0.8, denoise 0.35/0.65 each fail both teal preservation and teal-to-green edit; all preserve composited exterior exactly. Do not resample these variants. | None used. |
| completed | `illustrious_xinsir_strength_matrix` | Whether changing Xinsir control strength at d1.0 resolves target edit/no-change drift | Strength 0.4 and 1.0 both fail edit and no-change with pale-gray target; exact exterior composition remains intact. Do not resample those strengths. | None used. |
| completed | `illustrious_xinsir_d100_s080_seed_replication` | Whether the one d1.0/strength-0.8 green edit is reproducible rather than a seed outlier | The new paired seed fails edit and no-change: observed edit mechanics 1/2, target no-change 0/2. ADR-0014 blocks further same-route mask-boundary tuning. | None used. |
| completed | `hard_assertion_manifest_and_review_record_v1` | Whether a renderer-independent, case-scoped assertion/review artifact can carry intent separately from execution evidence | Manifest/review v1 now separates comic intent, execution hashes, hard assertion outcomes, human-minute state, and rejection decision without frozen-benchmark changes. | None used. |
| completed | `qwen_image_edit_2511_feasibility_and_control_protocol` | Whether multi-reference conditioning is a technically and policy-suitable next renderer mechanism | Apache-2.0 card/revision is source-pinned, but 53.75 GiB BF16 artifacts exceed the current 24 GiB local GPU before runtime overhead. Acquisition is deferred pending capacity/profile choice. | No model download used. |
| completed | `benchmark_case_bundle_v1_draft` | Whether the existing adapter-specific stage/control assets are explicit enough to support auditable future renderer comparisons | Versioned G07a/G07b stage controls, no-change/error-injection protocol, hashes, limitations, and freeze gates as `DRAFT_ADAPTER_SPECIFIC_G07_CONTROLS_NOT_FROZEN`. | None used. |
| completed | `native_repaint_provenance_validator_v1` | Whether execution provenance and assertion review records can be mechanically checked without leaking intent into execution | Validator passes all 10 native repaint records and their separate review while preserving the intent/execution boundary. | None used. |
| completed | `qwen_image_edit_2511_comfy_int8_preflight` | Whether a smaller, official local Qwen profile can be fully specified before acquisition | No-download profile pins artifacts, exact conditional dependency state, fictional-only protocol, and local VAE verification; it does not select/acquire the profile. | None used. |
| in progress | `cross_provider_fictional_g07_bakeoff_r1` | Which materially different API mechanisms improve two-role binding, set continuity, target change, and no-change behavior | Aggregate ledger concurrency passes; current official docs are recorded; all four credentials, frozen source hashes, data boundaries, $100 cap, and both BFL public-control hashes pass live no-provider preflight. Full-run maximum reservation is $4.20. | None for the authorized four-provider fictional-control run. Any expanded upload, managed GPU, large model, or cap increase remains gated. |
| 2 | `ch03_built_in_frontier_art_human_review` | Whether a visually promising, provenance-limited frontier-art service can supply research narrative panels under the current project acceptance rule | Three CH03 candidates and one rejected repair candidate are recorded. P002/P003 have preliminary visual passes; no panel has an authorized timed human review, reproducibility evidence, service-model pin, or commercial clearance. | Authorized human review only; no new provider spend or upload. |
| 2 | `chapter_scale_production_gate_r1` | Whether an execution arm has enough acceptance, provenance, and review evidence to justify expanding a short sequence toward 50–90 panels | CH03 and CH04 each have only 3 review-pending, unaccepted panels (47 below the lower target), even though their deterministic draft lint is clean. Do not expand the provenance-limited built-in service route to chapter scale before a controlled renderer comparison and human review establish a credible path. | None to maintain the gate; expansion depends on bakeoff/human-review evidence. |
| 2 | `vcs-baseline-governance` | Whether source revisions can become a first-class reproducibility field for selected renderer work | No Git worktree is detected, so hashes exist but a source commit cannot be recorded. ADR-0021 deliberately does not create a history without owner policy. | Owner decision on repository initialization/remote and history policy. |
| 1 | `chapter-scale-script-canon-decision` | Whether the existing Garden's Anchor pilot can become a current production planning source | It is a real chapter-scale local script, but its stated total is internally inconsistent (92 vs 52+44=96); lead names, chronology, canon, and photo-derived-design provenance also conflict with the current Soren/Sigrid system. ADR-0022 quarantines it. | Owner decision: explicit adaptation/mapping plus clean fictional-design basis, or retain it as historical reference and author a new chapter-scale current script. |
| 1 | `clean_ch05_mill_signal_review_and_plan_promotion` | Whether the new clean 50-panel Soren/Sigrid option can be promoted from development script into current canon and panel-plan work | `clean-ch05-mill-signal-r1.json` is intentionally non-canon and contains no renderable/commercial claim. Its starting beat is the existing CH04 smoke hook; it does not import historical pilot content. | Owner narrative/canon review before creating StoryState, AssetRegistry, ComicPanelPlans, or renders. |
| 2 | `qwen_image_edit_2511_managed_gpu_profile_selection` | Whether a self-controlled, reproducible multi-reference renderer is practical beyond 24 GiB VRAM | Source/license and local preflight are pinned; cloud-GPU data handling/cost needs current primary evidence. | Provider/profile and large artifact/cloud-compute choice. |
| 3 | `winning_renderer_hardening_r1` | Whether the best observed bakeoff arm can be made repeatable and repairable beyond one visual sample | Begins only after the cross-provider review selects an arm on measured assertions and cost/time. | Depends on the prior result; no adult likeness route implied. |
| 4 | `fictional_design_or_stage_assisted_actor_assets` | Whether a different controlled-asset mechanism avoids the legacy seated-plate furniture/prop leakage | Existing adult plates remain unsuitable; depends on a selected renderer profile. | None for fictional-only assets; adult likeness remains separate local approval. |

Each candidate needs a distinct render profile, exact source/license record, adapter-specific controls, and a Stage-A manifest before any comparison.
