# Current pipeline recommendation — 2026-09-01

## Recommendation

Adopt a two-layer comic execution research path:

1. Keep deterministic geometry/stage assets as the spatial authority for camera, layout, occlusion, and non-contact blocking.
2. Treat renderer-facing role markers as temporary diagnostic controls, not characters or visible final-art assets. The current best local diagnostic is the orange/teal non-figurative tile pair.
3. Keep `baseline_legacy` immutable as the failure-profile arm. Do not invest further in its neutral actor-plate capture profile for reusable seated-table assets.
4. Keep FLUX.2 Klein as a local renderer candidate for composition/reference-conditioning research, but not as validated targeted repair or character/role production infrastructure.

## Measured basis

| Route | What it establishes | Measured limitation | Current disposition |
| --- | --- | --- | --- |
| `baseline_legacy` Stage A | Existing reference failure profile | 0 accepted candidates / 24 generations; role, set, interaction, and child-safety failures | Frozen comparison arm only |
| Legacy sequential inpaint | Local multi-pass mechanics | Broad masks alter set material; 2/4 full blocking passes | Separate non-scoring repair hypothesis |
| Legacy adult actor plates | Neutral-background keying is mechanically possible for one Soren plate | Sigrid foreign-content leakage 4/4; Soren retains attached dark artifact after alpha key | Reject as reusable G07 actor-asset route |
| Deterministic geometry proxy | Exact spatial placement, occlusion, non-contact, bit-identical no-change | No identity/final-art evidence | Retain as spatial authority |
| FLUX.2 Klein reference edit | Local semantic reference edit | 83.58% changed pixels; no-change controls 84.26% / 86.19% changed | Not targeted repair |
| FLUX geometry circle/triangle markers | Coarse stage/side preservation | Exact token count 0/4; circle interpreted as head/body | Reject marker representation |
| FLUX geometry tile markers | Color count/order and stage layout 4/4 in paired control; fixed-ROI deterministic sensor also passes 4/4 | Marker morphology drifts chair-like 3/4; sensor is stage-specific | Retain only as diagnostic |

## Superseded immediate experiment sequence

1. Run only a bounded additional tile-marker diagnostic expansion if needed to estimate variance; its output remains non-scoring and proxy-only.
2. Build a canonical controllable stage/camera asset plan that can replace the proxy while retaining the same spatial-interface contracts. This is an asset/staging investment, not a claim that generated proxy panels are production art.
3. Before investing in adult identity assets or a large second renderer, decide between the model alternatives below with current primary license/model-card evidence recorded in the model registry.

The current ledger has 58 local renderer generations but no production-accepted output with timed human review. The next production demonstration must capture human minutes from the first candidate through accept/reject decision; otherwise accepted-panel-rate and labor claims remain unmeasured.

## Next renderer decision gate

| Candidate | Expected information gain | Local effort / risk | Recommendation |
| --- | --- | --- | --- |
| Illustrious XL v2 + newly authorized compatible fictional-design assets/masked-LoRA controls | Tests style-native role binding and mask locality, where the legacy arm is weak | Requires a large checkpoint and new compatible assets; existing Anima LoRAs do not transfer | Best next model experiment if the priority is comic-native art and controlled identity research |
| Qwen Image Edit 2511 | Tests multi-reference/edit capability directly | Large runtime footprint; current 24GB local feasibility is unproven; no adult reference upload | Higher upside but defer until compute/profile decision |
| Continue FLUX.2 Klein | Cheap local composition/control evidence | Known global edit drift and marker morphology drift | Continue only for geometry/diagnostic work, not as the sole long-term renderer |

No large additional model download, cloud spend, or external upload is initiated by this memo. The existing model/license registry remains the source for pinned local hashes and primary-source references.

## Update: Illustrious XL v2 + verified Xinsir repaint

Illustrious XL v2 is now locally installed from its pinned official revision. The installed Xinsir SDXL ProMax ControlNet exactly matches its official Apache-2.0 artifact. Both have only been used with fictional geometry controls; Illustrious's declared OpenRAIL-M terms remain a commercial/distribution review gate.

| Route | Measured result | Disposition |
| --- | --- | --- |
| Illustrious base img2img | 2/2 paired token/order/table passes, but no recurring-kitchen assertion; normal-denoise no-change changes 99.954% of pixels | Partial composition evidence only |
| Bare img2img + deterministic mask composite | Exterior can be held exact, but teal-to-green semantic edit fails | Reject as semantic repair |
| Illustrious + Xinsir native repaint + composite | One target teal-to-green edit succeeds; composite exterior changes 0 pixels | Current strongest local repair-mechanics route |
| Same Xinsir route, unchanged-target prompt | Target teal token changes to pale gray despite exact exterior | Not yet a reliable repair route |

The selected OpenAI hardening branch now has a measured local boundary policy: a 16-pixel inward cosine feather is the only tested width meeting the predeclared discontinuity/core/exterior/lettering rule. It remains proxy-only and unaccepted; topology on the abstract P036 causal mask is the next gate before any narrative-panel applicability claim.

That first P036 topology gate reveals the existing target-context mask is a filled rectangle. It retains 79.032% core under the policy but cannot establish thin or concave causal-object behavior. Narrative applicability remains gated on a deterministic causal-shape control rather than inferred from the rectangle.

The follow-on causal-shape control exercises a concave plank/reach/hand/tin union and selects 8px context padding by connectivity/core/lettering rules. All four labeled features retain useful fully replaced cores with exact exterior mechanics. This supports a versioned local repair-policy contract, but the proxy mask remains categorically ineligible as a production mask or upload.

That contract is now versioned and bound into offline preflight. It preserves the four independent production gates and blocks proxy promotion, so the measured mechanics improve implementation readiness without changing the absence of approved CH05 inputs, upload scope, production budget, or executor.

P036 readiness r2 now packages those facts immutably while preserving r1. It still reports four blockers and no request capability. The next useful compiler measurement is chapter-wide policy coverage, not a provider call or an inference that other panels inherit P036's mask mechanics.

Chapter-wide coverage now identifies four explicit causal panels while binding the existing policy only to P036. Three policy-absent panels are candidates for a separate information-gain analysis; none is selected by motion mode or story appeal, and all production gates remain closed.

The predeclared low-denoise and strength matrices plus a paired d1.0/strength-0.8 replication now close this route: target edit passed only 1/2 observed seeds and target no-change passed 0/2. ADR-0014 rejects mask-boundary optimization until a different renderer mechanism proves both assertions.

The target/no-change matrices and paired replication are now complete. ADR-0014 rejects further same-route mask-boundary tuning; ADR-0015 requires a distinct renderer mechanism. The current recommendation is in `docs/research/renderer-decision-memo-20260901.md`.
