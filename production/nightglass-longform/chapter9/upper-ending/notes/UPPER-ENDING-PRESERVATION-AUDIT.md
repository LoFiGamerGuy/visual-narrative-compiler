# Chapter9 upper-ending preservation audit

PASS for exactly four canonical chains: 39–40 P/R1 and 41–42 P/R1. Two originals, each with one primary and its sole repair; zero finishes or pending calls. No original-unit cap reset or duplicate call was found in the scoped directory.

Each exact request JSON matches recorded arguments; prompt text/hash and every actual reference path/hash match. All four preserved default PNGs, local native PNGs and decoded two-field raw-return image payloads are byte-identical. Unknown model snapshot, seed, billing and owner approval remain null. Recorded submission/return timestamps are present and ordered; no inaccessible backend or independent network timing is inferred.

All four selected crops match their declared source hashes and exact rectangles in mode, dimensions and every pixel. Failed primary third-arm/wrong-sleeve pixels remain preserved. Preservation checks do not convert those visual failures into passes. Detailed file hashes and finite scope are in the adjacent JSON receipt.

Root owns the complete Chapter9 count (reported 38 calls, 24P/14R); this independent audit covers only the four ending chains. No artwork, raw return, default source, shared selection or reader was modified.
