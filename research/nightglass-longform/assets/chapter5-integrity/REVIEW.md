# Chapter5 preserved provenance and resource audit

PASS across all seven production folders. `audit.json` records each actual canonical call, argument/reference binding, native original and selected source; `audit.py` is read-only outside this report directory. No shared selections, original call records, readers, ZIPs or extractions were edited.

| Scope | Returned calls |
| --- | ---: |
|lead|20|
|crate-bindery|4|
|ilen-establishing|1|
|loft-delivery|6|
|loft-departure|1|
|ninth-return|4|
|side-25|1|
|Total|37|

The37 calls comprise27 primaries and10 structural repairs, zero pending calls, recorded tool failures or finishes. Every one of27 original image units has exactly one primary and at most one R1; each repair references its actual original primary. There are37 canonical candidate PNGs and no orphan candidate or renamed extra-primary unit. The lead's existing helper covers20 calls; this audit covers all37 without running that helper or writing its report.

Against the accepted pre-Chapter5 checkpoint179, this reconciles216 lifetime invocations and104 remaining within the original320. Chapter5 used37 of its65-call planning ceiling, leaving28 unused; neither ceiling is a target or a new tranche. Counts are based on preserved canonical records and the accepted earlier checkpoint, not invented billing telemetry.

Every candidate matches its preserved generated original, stored native SHA and decoded raw tool-return image bytes. Exact canonical argument JSON, embedded call arguments where present, submitted prompt hashes where recorded, reference list ordering/paths and actual reference hashes agree. All37 preserve null model snapshot, seed and billing; absent/null owner approval remains unknown. Auxiliary return metadata, when present, matches output hints and image URL lengths.

Two minor archival nuances are retained candidly. N5-07-P and08-P auxiliary prompt TXT files contain one additional trailing LF; canonical request JSON, embedded submitted arguments and prompt SHA agree exactly. The initial overly strict auxiliary-TXT assertion and bounded harness correction are preserved in `initial-harness-observation.json`; no production file changed. N5-31-32-P's `submitted_utc` was written during preparation before the usage pause. Its exact real invocation start is explicitly unknown/null, with one invocation and return afterward. Return timestamps describe completed local preservation, not provider duration.

All42 selected panel file hashes and every selected crop's source hash/dimensions/pixels pass. N5-01 is the accurate existing N3-04 crop reuse; N5-09 is a lossless detail of actual08, with no separate image call. All other selections resolve to recorded Chapter5 natives. The untouched companion panels22/24/26/35 remain sourced from their original primaries despite repairs to adjoining tiers. All17 recorded reference derivatives across Chapter5 also pass source/hash/lossless pixel checks. The42 selected art records stayed unchanged during the audit.

This is a provenance/resource audit, not a fresh visual acceptance or complete phone reading. At audit start, `reviewed_complete` was still `[1,2,3,4]`; the Chapter5 full-reading gate is separate. Actual narrative inspections remain in lead/worker/reader review records, including corrected laterality, palette and timing qualifications. Package build is held until root releases that gate and a stable source snapshot.
