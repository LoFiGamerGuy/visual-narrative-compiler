# Verifier modal capture fix — browser-only preflight

PASS. `production/nightglass-longform/package/verify_package.py` now scrolls the actual `dialog[open]` for historical options/anchors captures. It records each top/middle/end offset, scroll/client height, image count, broken-image list and overflow, and checks each Close control. Wrong78/9 counts, failed scrolling, broken images, overflow or a failed close cause verification failure. Modal image/link checks are scoped to the dialog rather than mixed with the underlying gallery.

Regular document scrolling/captures and dynamic chapter route discovery remain unchanged. The test calls the updated `browse()` directly on the already hash-verified immutable FOUR-chapter extraction; it does not extract or rebuild anything. `run.py` and `receipt.json` preserve the exact test and result. Bound fourchapter ZIP SHA256: `e1d3da0fe6c897e804e2e93a9b7fbcd4c630150c7f6f21370f1059dfaf434833`. Tested verifier SHA256: `51839fa317168db31114a4191c861da496f0735ad06e03b867511018df71054b`.

All18 routes passed at390×844 over file://, with no broken images, missing links, browser errors, external requests or overflow. All16 regular-route data records equal the prior verifier's records exactly, and all46 regular-route screenshots are byte-identical (`regular-capture-comparison.json`). This compact comparison is not a claim of fresh visual inspection of those46 images.

I actually viewed all SIX new historical modal captures under `phone-390/`: options top/middle/end and anchors top/middle/end. The real internal offsets are options0/18403/36806 and anchors0/1502/3004. All78 options images and9 anchors loaded. Both Close controls worked. The art, labels and provenance are readable; adjacent historical source links retain their existing tight spacing. Repeated modal tops from the old document-scroll method are not counted as interior coverage.

This is preparation for the next package, not evidence that a five-chapter package exists or is portable. The tested fourchapter extraction yields18 dynamic routes; a complete fivechapter snapshot will add its chapter route through the existing discovery logic. ZIP, extraction, source readers and shared selections were not modified. No art calls or additional framework were introduced.

Lead review: root inspected the scoped verifier diff and actually viewed ALL SIX new modal top/middle/end PNGs. Distinct interior and final content is visible with readable labels and the same historical tight link spacing. The checks bind genuine dialog scrolling and close behavior; accepted for future package verification. Regular-route byte comparison plus the earlier actual fourchapter package reading supports unchanged-page behavior, not a claim of newly reading those46 images. Fivechapter final ZIP still requires complete42-panel source and fresh extraction/hash/browser verification.
