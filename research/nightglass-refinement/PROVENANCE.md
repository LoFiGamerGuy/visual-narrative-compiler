# Native art and reproducible evidence

Experiment NR-20260908-01 starts from WC commit ccc23de8dec25c12b0a2d85fd17410d526e62468 in an isolated sibling worktree/branch. The owner’s latest feedback is preserved verbatim in production/nightglass-refinement/owner-feedback.json. It is overall praise plus a texture concern, not per-image votes. The exact earlier CE selection export remains separate and validated against its dataset, plan and18 actual native bindings.

The frozen full plan is production/nightglass-refinement/refinement-plan.json, SHA-256 bbf9219ce672e5b23959cffc01f9baab0e90e71fbdc8cf508a40a248afd7d653. The proof plan, pre-output independent rubric, source bindings and actual native/phone evidence are preserved. texture-gate.json records the technical4/4 gate and explicit limits. Twenty expansion compositions follow the frozen concepts; prompt order and surface emphasis were tightened after visible primary drift, documented in prompt-refinement-v2.md. Every exact submitted prompt remains in prompts/ and frozen job arrays.

There are24 primary calls and six repairs, at most one repair per image, no pre-generation rejection and no additional image call. The built-in image-generation tool was used exclusively. No paid API calls or model purchases were made. model, snapshot, seed, token usage and billing metadata were not exposed and remain null. Direct paid spend is recorded as zero; exact pixel regeneration from prompts is not promised.

production/nightglass-refinement/calls contains start/end receipts, exact input paths and SHA-256 hashes, prompt hashes, retry reasons, native return locations and tool output hints. candidates.json binds each copied PNG to its original native source. selected.json chooses the displayed attempt without any owner approval. review-notes.json binds technical limitations to the actual displayed hashes. All30 original tool images are preserved, with byte-identical local copies in candidates/; no raster postprocessing was used to manufacture repairs or previews. Browser screenshots are separate review evidence, not native art.

The new package includes all30 attempts, nine original full anchor copies,18 CE selected native copies and exact export,24 WC selected native copies and metadata, owner feedback, exact prompts/references/provenance, independent reviews and offline source. Original tool storage and full earlier workspaces remain in place. Runtime caches, browser profiles, scratch data, unrelated assets and the ZIP itself are excluded from its own contents. Native PNGs/ZIP are intentionally ignored by Git; committed source and the verified portable ZIP together form the delivery.

The final reader dataset is f6d126bf7ded8e91b23f78ceedcb01e192e611a883be57558e387f848d20a218. Every new binding contains selected attempt/hash and, for matched studies, exact prior before attempt/hash. Both sides participate in dataset identity. All new choices start blank; prior choices and global feedback stay read-only. Export/import rejects stale or mismatched bindings atomically.

Preservation checks compare17 pre-existing worktrees’ HEAD/status, all pre-existing refs and2717 protected files including earlier native deliveries and the previous verified ZIP. The baseline capture timing note is retained honestly in protected-initial.json; only this new branch is excluded from prior-ref comparison. These checks do not claim exhaustive hashing of unrelated model caches. Final verification, portable receipts, browser evidence and local/remote parity records document the completed state.
