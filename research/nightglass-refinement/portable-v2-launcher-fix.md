The final-v2 archive passed399-file extraction/rebuild and actual offline browser checks. Completion audit then found that ACTIVE_PIPELINE/START_HERE advertised docs/nightglass-refinement/OPEN_GALLERY.cmd, but only the portable root launcher existed. Added the small docs-local launcher pointing at its sibling index.html. Root and docs launchers now resolve to the same real gallery. Preserve verifiedv2; final-v3 includes the advertised launcher and updated pointers. No image, dataset or preference changes.
