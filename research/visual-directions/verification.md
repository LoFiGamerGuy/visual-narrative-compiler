# Verification of the twenty-direction study

The closed study contains 20 original concepts and 21 actual image-tool returns: 20 primary boards plus one targeted correction of option 19's generated signature-like artifact. The primary remains preserved. Twenty images are selected for display; that selection is an editorial file choice, not owner approval. All native returns are 1536×1024. Direct paid spend is $0; underlying raster model, seed, usage and billing allocation are unknown.

`python research/visual-directions/verify_artifacts.py` passed. It verifies both frozen brief hashes, every exact prompt hash, all 21 copied PNGs against retained tool originals, dimensions, metadata, reference hashes and the complete 20-option selection. Its source-bound result is [results.json](results.json).

The lead and [independent art reviewer](independent-review.md) inspected all 20 primary images and the correction at native size. The corrected 19 removes the definite signature-like defect without an obvious material design regression. A generative edit may alter fine detail elsewhere; no exact pixel-locality claim is made. The selected set has meaningful character, silhouette, rendering, world and creature variation. Broader medium families overlap, and the gallery labels actual hybrid outputs honestly. No owner favorite or sequence consistency has been established.

The [gallery browser receipt](gallery-browser-qa.json) and [overview receipt](overview-browser-qa.json) pass at 390×844, 1024×768 and 1440×1000. All 20 boards decode without cropping, pending slots, page overflow or console errors. Fit/native zoom, nested Escape, two-to-four comparison, separate component likes, favorites, notes, filters, storage fallback, actual JSON download/import/reload and eight invalid import rejections pass. All image hashes and local links resolve. Eight isolated builder tests pass in [gallery-builder-tests.txt](gallery-builder-tests.txt). The lead additionally viewed the final phone screenshot and both complete overview sheets; their captions and option ordering are legible.

Final display dataset: `d717a61cc7c4793bbfc2a878041666179ba03dfd17a0aa07079a5d4d547ad8f7`. The source bindings in the browser receipts were checked against the final files before commit.

[Preservation comparison](protected-final.json) passes for all 12 prior worktrees' HEADs/statuses, pre-existing refs and the declared 1,105 prior structural files' bytes and metadata. Scope and exclusions are in [the initial baseline](protected-initial.json); this is not a whole-machine or model-cache audit. This goal did not run Blender or mutate a protected tree.

Native PNGs and the two overview sheets are delivered in the local ignored [artwork bundle](asset-bundle/README.md); Git contains prompts, code and evidence. The bundle's manifest and receipts record exact native dimensions, hashes and restoration checks. Post-commit fresh-Git restoration and remote parity evidence are kept in the bundle's ignored `local/` directory and this namespace's ignored `.scratch/` directory so recording delivery does not change the delivered commit.

Re-running the result or preservation writers refreshes their timestamped JSON reports; for a read-only audit, compare the committed report hashes and use a separate output path where the helper supports one.
